# CCMB 마무리 절차

## 검증 범위 원칙

- SwiftPM 명령은 Codex 샌드박스와 SwiftPM 내부 샌드박스의 충돌 및 사용자 홈 캐시 권한 오류를 피하기 위해 반드시 `./Scripts/swiftpm.sh test`, `./Scripts/swiftpm.sh build`처럼 공통 래퍼로 실행한다. `swift test`, `swift build`, `swift package`를 직접 실행하지 않는다.
- 일반적인 국소 변경은 변경과 직접 관련된 테스트와 빌드 검증을 통과하면 설치할 수 있다.
- 전체 테스트는 배포 전, 여러 핵심 모듈을 동시에 변경한 경우, 공통 기반 코드·외부 의존성을 변경한 경우, 관련 테스트만으로 영향 범위를 판단하기 어려운 경우 또는 사용자가 요청한 경우에만 실행한다.
- 반복 수정 과정에서는 실패했거나 변경과 관련된 검증만 다시 실행하며, 이미 통과한 전체 검증을 이유 없이 반복하지 않는다.
- 완료 보고에는 실행한 테스트 범위와 생략한 검증을 명확히 적는다.

## 개발 속도·검증 효율·Mac 자원 보호

- 작은 수정마다 빌드·테스트하지 않고, 컴파일과 검증이 가능한 하나의 의미 있는 변경 묶음이 완성된 뒤 검증한다.
- 변경 전 기준 테스트는 버그 재현, 현재 기준 상태가 불명확한 경우, 공통 기반 변경에만 실행한다. 일반 변경에는 읽기·정적 분석을 우선한다.
- 같은 소스 상태, 같은 테스트 선택자, 같은 실행 환경에서 통과한 검증은 다시 실행하지 않는다. 소스·테스트·빌드 설정·실행 환경 중 관련 항목이 바뀐 경우에만 재검증한다.
- 검증은 `변경 관련 테스트 → 빌드 → 필요한 실행 확인` 순서로 최소 범위부터 넓힌다. 전체 테스트는 배포 후보, 공통 기반·외부 의존성 변경, 영향 범위가 불명확한 변경 또는 사용자 요청 시 한 번만 실행한다.
- 같은 인프라 오류가 두 번 반복되면 같은 명령을 다시 실행하지 않고 원인을 진단한다. 실패한 검증을 수정한 뒤에는 실패 항목과 그 영향 범위만 다시 확인한다.
- 여러 에이전트나 작업이 같은 저장소, 빌드 시스템, 시뮬레이터 또는 실기기를 동시에 사용하지 않는다. 병렬화는 읽기 전용 조사처럼 자원 충돌이 없는 작업에만 사용한다.
- 빌드·테스트 전 데이터 볼륨의 여유 공간을 확인한다. 여유 공간이 50GiB 미만이거나 전체 용량의 10% 미만이면 새 대형 빌드·전체 테스트·시뮬레이터 생성을 시작하지 않고 사용자에게 보고한다.
- 작업 전용 DerivedData, 빌드 캐시, 결과 번들, 임시 디렉터리는 명시적인 작업별 경로를 사용하고 성공·실패·중단 후 더 이상 필요 없으면 안전하게 정리한다. 사용자 공용 캐시나 다른 작업 산출물은 임의로 삭제하지 않는다.
- Mac의 디스크, 메모리, CPU, 배터리, 스왑, 시뮬레이터·에뮬레이터와 연결 기기는 사용자의 자원이다. 장시간 고부하, 무제한 프로세스·복제 기기·로그·캐시 누적을 허용하지 않는다.
- 진행 확인이나 상태 보고만을 위해 빌드·테스트를 실행하지 않는다. 이미 확인한 결과와 로그를 재사용하고, 로그는 원인 확인에 필요한 범위만 읽는다.

- 사용자가 CCMB 작업에서 `마무리`라고 하면 다음 순서 전체를 수행한다.
  1. 소스 코드 오류를 확인하고 적절한 빌드와 검증을 통과시킨다.
  2. `Products`의 결과물과 `/Applications/CCMB.app`을 검증된 최신 파일로 교체한다.
  3. `/Applications/CCMB.app`을 재실행하고 정상 동작을 확인한다.
  4. 변경 범위와 비밀정보 포함 여부를 확인한 뒤 GitHub에 커밋하고 현재 브랜치를 푸시한다.
- 사용자가 `마무리는 이따`, `아직 마무리하지 마` 또는 같은 의미로 말하면 위 절차를 시작하지 않는다.
- `마무리` 단축 지시를 받지 않은 일반 수정에서는 설치·재실행·커밋·푸시를 자동으로 수행하지 않는다. 각 외부 단계는 사용자의 명시적 요청이 있을 때만 수행한다.

# 주간 사용량 절약 원칙

- 모든 에이전트는 가능한 한 주간 사용량을 아끼며, 사용자 요청을 완료하는 데 필요하지 않은 작업은 하지 않는다.
- 이미 확인한 내용을 반복 조사하거나, 요청 범위를 벗어난 탐색·분석·리팩터링·도구 호출을 하지 않는다.
- 독립적으로 처리할 실질적인 이점이 없는 경우 에이전트를 추가로 생성하거나 작업을 불필요하게 분산하지 않는다.
- 필요한 정보가 충분하면 추가 확인을 반복하지 않고 가장 직접적인 방법으로 작업을 완료한다.
- 사용량 절약을 이유로 사용자 요구사항, 안전 확인, 기존 기능 보호, 필수 검증을 생략하거나 검증 수준을 낮추지 않는다.
